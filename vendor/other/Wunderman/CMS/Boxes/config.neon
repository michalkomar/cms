parameters:
	PrivateModule:
		AddButtons:
			## This name must be same as name of injected service in Wunderman\CMS\Boxes for composedPagePresenter (@privateModuleService in this config)
			boxes:
				text: 'Add boxes'
				tooltip: 'If You need to add any box you must first create them in the Page parts.'
		PagePartsButtons:
			':Private:Boxes:List:':
				text: Boxes
	renderableComponents:
		boxes:


services:
	privateModuleService:
	 	class: Wunderman\CMS\Boxes\PrivateModule\Service